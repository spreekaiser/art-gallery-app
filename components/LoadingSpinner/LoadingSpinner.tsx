import { StyledSpinnerContainer, StyledSpinner } from "./LoadingSpinner.style";

export function LoadingSpinner() {
  return (
    <StyledSpinnerContainer>
      <StyledSpinner />
    </StyledSpinnerContainer>
  );
}
